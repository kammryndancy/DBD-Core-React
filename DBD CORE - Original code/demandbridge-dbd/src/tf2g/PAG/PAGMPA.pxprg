0010 REM "Accounts Payable Gateway - GAP <PAGMPA>
0035 REM "5.7 - 04/13/15 - 17.028055 - tma - SSP# 276476
0037 REM "276476-GAP P1,P2,P3 and S1 displays large red box                  
0040 REM "Copyright 2015 Demand Bridge, LLC.; Norcross, Georgia
0041 REM "        Licensed Software - All Rights Reserved.
0050 IF TCB(13)=1 THEN BEGIN 
0052 IF TCB(88)=0 THEN MSGBOX "You must be using Windx to access this program"; GOTO 0075 ! SSP#276476
0055 IF NOT(%GUI) THEN CHUI_MODE=1; %GUI=1; PRINT 'SHOW'(0)
0060 PROCESS "PAGMPA","../PAG/PA.EN"
0065 IF CHUI_MODE THEN %GUI=0; PRINT 'SHOW'(1); RUN "ZMENU"
0075 IF TCB(13)=1 THEN RUN "ZMENU" ELSE EXIT 
0090 ! 
0100 ! 100 - Initialization
0110 INIT:
0120 ! 
0130 X0$="PAGMPA",X1$="Accounts Payable Gateway Parameters"
0140 ! 
0150 X0=-1,X2=-1
0160 CALL "ZZCOMP",X0$,X1$,X2$,X3$,X4$,X5$,X0,X1,X2; IF X1>0 THEN GOSUB WRAPUP
0170 ! 
0180 K9$=X3$(9,3)+"GAP",K9=LEN(K9$)
0190 DIM Z[NUM(X3$(60,3))]
0200 ! Files
0210 Z$="01X ZZPARM  06O ZZPARM  "
0220 CALL "ZZFLES",X3$,Y1$,Y0$,Z$,Z{ALL},Z0,0
0230 GOSUB READ_INFO
0300 ! Iolist Section
0310 IOLIST PA$(1),PA[0],PA[1],PA[2]
0490 RETURN 
0500 ! 500 - Main panel post_display logic
0510 MAIN_POST_DISPLAY:
0780 REFRESH_FLG=1
0790 RETURN 
0900 ! 900 - Wrapup
0910 WRAPUP:
0920 CALL "ZZFLES",X3$,Y1$,Y0$,"END",Z{ALL},0,0
0970 RETURN 
1000 ! Get XML Parameter Information
1010 READ_INFO:
1020 DIM PA$(1024),PA[2]
1030 READ (Z[1],KEY=K9$,DOM=ADD_MSG)IOL=0310
1050 B_PA$=PA$
1090 RETURN 
2000 ! Load Screen with Parameter Information
2010 LOAD_SCREEN:
2020 PAG.A001$=PA$(7,60)
2080 REFRESH_FLG=1
2090 RETURN 
3000 ! Write the record
3010 WRITE_REC:
3030 IF PA$=B_PA$ THEN RETURN 
3040 WRITE (Z[6],KEY=K9$)IOL=0310
3080 B_PA$=PA$
3090 RETURN 
4000 ! Load Parameter string with user input from screen
4010 LOAD_INFO:
4020 PA$(7,60)=PAG.A001$
4080 B_PA$=PA$
4085 WRITE (Z[6],KEY=K9$)IOL=0310
4090 RETURN 
5000 ! Check Changes
5010 CHECK_CHANGES:
5020 PA$(7,60)=PAG.A001$
5030 IF B_PA$=PA$ THEN RETURN 
5040 MSGBOX MSG("REC_ALTERD"),MSG("UPDATED"),"?,YESNO",ANS$
5050 IF ANS$="NO" THEN RETURN 
5060 GOSUB WRITE_REC
5090 RETURN 
8000 ! Add Messages
8010 ADD_MSG:
8020 MSGBOX MSG("ADD_RECORD"),MSG("VERIFY"),"?,YESNO",ANS$
8030 IF ANS$="NO" THEN CMD_STR$="END"; RETURN 
8040 DIM PA$(1024); B_PA$=PA$
8045 PA$(1,6)=K9$
8050 NEXT_ID=PAG.A001.CTL
8090 RETURN 
9999 END 
56002 REM "262361-GUI conversion of Accounts Payable Gateway - Module GW_A/P
