0010 REM "Update OS2 RECORDS from text file  <OS2UDB>
0020 SETESC 9300; SETERR 9000
0035 REM "5.4 - 01/16/06 - 11.112777 - lms - SSP# 177202
0037 REM "177202-On the shopping cart page, the EC-Promo and TOPS item       
0040 REM "Copyright 2006 TopForm Software Inc.; Norcross, Georgia
0041 REM "        Licensed Software - All Rights Reserved.
0090 CLEAR ; SETERR 0100; ENTER X3$,X4$,Q0$,Q1$
0095 F$="CALLED",FF$=F$
0100 IF %GUI=0 THEN SETERR 9000
0110 X0$="OS2UDB",X1$="Update image name into OS2 file from Text File"
0120 DIM Z0$(80,"-")
0125 M0$="#######.00"
0130 K0=20,K1=1
0135 C9=-1
0200 REM "
0240 IF X3$="" THEN CALL "ZZCOMP",X0$,X1$,X2$,X3$,X4$,M9$,X0,X1,X2; IF X1>0 THEN GOTO 9920
0245 IF X3$(66,1)>"2" THEN PRECISION NUM(X3$(66,1),ERR=0246)
0295 DIM Z0$(80,X3$(235,1)); Z0$=MNM('GS')+Z0$+MNM('GE')
0298 IF TCB(88)=0 THEN CALL "ZZPROM",".4",X3$,Z,"This program MUST be run using WindX...","","",0; RUN "ZMENU"
0299 IF %GUI=0 THEN CALL "ZZ2PRP","OS",RESULT$,DATE$; IF RESULT$<>"Y" THEN CALL "ZZPROM",".4",X3$,Z,"ACCESS DENIED! The Office Supply Interface System is not an i n stalled module.","","",0; RUN "ZMENU"
0300 REM "IOLISTS
0320 IOLIST OS0$
0321 IOLIST OS1$
0322 IOLIST OS2$
0325 IOLIST OS7$
0400 REM "
0500 REM "FILES
0505 IF %GUI=0 THEN DIM Z[NUM(X3$(60,3))]
0510 Z$="02O OSD...  03O OS2...  13O ZZPARM  "
0520 IF %GUI=0 THEN CALL "ZZFLES",X3$,Y1$,Y0$,Z$,Z{ALL},Z0,Z1; ON Z0 GOTO 0521,9900
0555 IF %GUI THEN GOSUB 8100; GOTO 0700
0560 OPEN (10,OPT="TEXT",ERR=9000)"images.txt"
0600 REM "
0610 IF %GUI=0 THEN GOSUB 6000
0620 GOSUB 8100
0640 IF Q1$="" OR %GUI=0 THEN CALL "ZZPROM",".Y",X3$,Z,"Proceed?","","",0; ON Z GOTO 0641,9900
0700 I$=Q0$
1000 REM "
1010 READ (10,END=5000)SEGMENT$
1012 SEGMENT$=STP(SEGMENT$,3,QUO)
1014 ITEM$=SEGMENT$(1,15)
1015 IMAGE$=SEGMENT$(17)
1017 C=C+1; IF MOD(C,T0)=1 THEN GOSUB 8150
1020 READ (Z[2],KEY=ITEM$,DOM=*NEXT)
1025 OSD_KEY$=KEY(Z[2],END=1000)
1030 IF OSD_KEY$(1,15)<>ITEM$ THEN GOTO 1000
1040 READ (Z[3],KEY=OSD_KEY$(22,10),ERR=1000)OS2$
1050 OS2$(61,15)=IMAGE$
1060 IF %GUI=0 THEN PRINT @(25,10),OS2$(1,10),"  ",OS2$(11,15)
1070 WRITE (Z[3],KEY=OSD_KEY$(22,10))OS2$
1075 REM "OS2_COUNT=OS2_COUNT+1; PRINT @(10,40),OS2_COUNT
1100 GOTO 1000
5000 REM "EOJ"
5010 IF %GUI THEN GOTO PROCESS_END
5030 CALL "ZZPROM",".4",X3$,Z,"Process complete!!","","",0
5040 GOTO 9900
6000 REM "BACKGROUND
6005 PRINT (0,ERR=6016)'SB',
6020 PRINT @(0,4),"This program will read through the Office Supply Database",@(0,5),"and update the Image Names into the OS2 File."
6165 PRINT (0,ERR=6166)'SF',
6190 RETURN 
8100 REM "GOSUB here, once, at the beginning, after the background is set and before the Proceed? question. Set T to total # to do (by calling ZZINFO) and T0 to the reporting interval (report every T0 number of records) this is typically 2% of total; This also prints a message (@8115),so adjust accordingly
8110 CALL "ZZINFO",Z[2],T9,X3$,T,T0,K,B,D,S0,S1,F$
8115 IF GUI=1 THEN ML_MESS.CTL'VALUE$="There are "+STR(T)+" records to process"
8130 T0=INT(T*.02); IF T0<=1 THEN T0=2
8145 RETURN 
8150 REM "Call this each time to update the bar graph. Display horiz. bar graph given total #, T and current #, C, and time counter T1 (T1 is init'ed here and used here, just don't use it somewhere else)
8151 REM "line reset to 21 from 19 for ssp#089855 cwj 7/15/98
8153 IF %GUI=0 THEN CALL "ZZBARG",X3$,"H",21,10,50,T1,T,C
8155 IF %GUI THEN CALL "ZZBARG",X3$,"HG",6.2,1,28,T1,T,C
8195 RETURN 
9000 REM "ERROR PROCESSING
9005 IF ERR=69 THEN GOTO 9500
9010 Y5=ERR,Y6=TCB(5)
9015 SETERR 9016; Y8$=LST(PGM(Y6))
9016 SETERR 9000
9040 CALL "ZZERRM",Y8$,X0$,Y7$,X3$,Y5,Y6,Y7,Y8,0
9045 REM 
9050 ON Y7 GOTO 9060,9100,9800,9070,9090
9055 REM 
9060 RETRY 
9070 SETERR 9080
9075 EXECUTE Y7$
9080 SETERR 9000; RETRY 
9090 SETERR 0000; RETRY 
9100 REM " TRANSFER CONTROL
9180 GOTO 0990
9190 GOTO 9800
9300 SETESC 9350
9310 SETERR 9350
9350 SETERR 9000; RETURN 
9500 REM "CTRL LOGIC
9510 SETERR 9000; GOSUB 6400
9520 ON C9 GOTO 1140,2040
9900 REM "END PROGRAM
9910 CALL "ZZFLES",X3$,Y1$,Y0$,"END",Z{ALL},0,0
9930 SETERR 9940; Q1$=A1$; EXIT 
9940 SETESC 9350
9950 RUN "ZMENU"
9999 END 
20000 ! 100 - Initialization
20010 INIT:
20015 CALL "ZZCOMP",X0$,X1$,X2$,X3$,X4$,M9$,-1,X1,X2; IF X1>0 THEN CMD_STR$="END"; GOSUB WRAPUP; GOTO *RETURN
20020 _SV_KR=PRM('KR'); SET_PARAM 'KR'=0 ! Ensure native handling of KEP()
20027 IF POS("MS"=UCS(SYS)) THEN ISWIN=1 ELSE ISWIN=0
20030 GOSUB SETUP_LIBRARY; IF %FLMAINT_MSG$<>"" THEN MESSAGE_LIB %FLMAINT_MSG$
20055 CALL "ZZ2PRP","OS",RESULT$,DATE$; IF RESULT$<>"Y" THEN MSGBOX "","TOPS Module is not installed"; CMD_STR$="END"; GOSUB WRAPUP; GOTO *RETURN
20082 DIM Z[NUM(X3$(60,3))]
20085 Z$="02O OSD...  03O OS2...  13O ZZPARM  "
20087 CALL "ZZFLES",X3$,Y1$,Y0$,Z$,Z{ALL},Z0,Z1; IF Z0>0 THEN GOTO OPEN_ERR
20089 OPEN (10,OPT="TEXT",ERR=9000)"images.txt"
20090 RETURN 
20100 OPEN_ERR: MSGBOX _MSG_FILOPNERR1$+QUO+_FILE_NAME$+QUO+SEP+_MSG_DIRECTORY$+LWD+SEP+_MSG_PREFIX$+PFX,MSG(ERR),"!"
20110 CMD_STR$="END"
20120 RETURN 
20130 ! 300 - Set up the message library
20140 SETUP_LIBRARY:
20150 _SV_MSGLIB$=MSG(*)
20160 _LANG$=LCS(%NOMAD_DEF_SFX$); IF _LANG$="" THEN _LANG$=LCS(ENV("PVXLANG")); IF _LANG$="" THEN _LANG$=LCS(ENV("LANG")); IF _LANG$="" THEN _LANG$="en"
20170 MESSAGE_LIB "*tfmsglib."+_LANG$,ERR=*NEXT; GOTO 20190
20180 MESSAGE_LIB "*tfmsglib.en"
20200 RETURN 
20350 ! 900 - Wrapup
20360 WRAPUP:
20370 %NUM_OS0_REC$=""
20400 MESSAGE_LIB _SV_MSGLIB$
20410 SET_PARAM 'KR'=_SV_KR
20415 CALL "ZZFLES",X3$,Y1$,Y0$,"END",Z{ALL},0,0
20416 CLOSE (10,ERR=*NEXT)
20417 CMD_STR$="END"
20420 RETURN 
34000 ! 14000 - Enable/Disable groups - _ENABLE_FLG: 0=don't!, >1=corresponding key segment on, others off, -1=buttons and non-key fields on
34010 ENABLE_GROUPS:
34030 CALL "*wingrp;Disable",BUTTONS.GRP$
34040 CALL "*wingrp;Enable",FIELDS.GRP$
34050 RETURN 
36100 GET_RECORDS:
36110 CALL "ZZINFO",Z[2],T9,X3$,T,T0,K,B,D,S0,S1,F$
36120 %NUM_OS0_REC$=STR(T)
36190 RETURN 
36200 PROCESS:
36220 GOTO 0100
36250 PROCESS_END:
36255 MSGBOX "Process Complete!!",MSG("CONFIRMING"),"INFO,TIM=3"
36260 GOSUB WRAPUP
36290 RETURN 
