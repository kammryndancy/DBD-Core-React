0010 REM "Repository Document Removal <ZR2UT2>
0020 SETESC 9300; SETERR 9000
0035 REM "5.7 - 02/03/10 - 9.705 - tma - SSP# 236018
0037 REM "236018-Repository Document removal - Verbage change                
0040 REM "Copyright 2010 Demand Bridge, LLC.; Norcross, Georgia
0041 REM "        Licensed Software - All Rights Reserved.
0090 CLEAR ; SETERR 0100; ENTER X3$,X4$,Q0$,Q1$
0100 SETERR 9000
0110 X0$="ZR2UT2",X1$="Repository Document Removal"
0120 DIM Z0$(80,"-")
0130 K0=20,K1=1
0135 C9=-1
0150 DIM A[4],C[4]
0165 DEF FND$(Z9$)=Z9$(NUM(X3$(48,1))*2+1,2)+X3$(59,1)+Z9$(7-NUM(X3$(48,1))*2,2)+X3$(59,1)+STR((ASC(Z9$(1,1))-65)*10+1900+NUM(Z9$(2,1))-1570*POS("  "=Z9$(1,2)):"####")
0200 REM "
0240 CALL "ZZCOMP",X0$,X1$,X2$,X3$,X4$,M9$,X0,X1,X2; IF X1>0 THEN GOTO 9920
0245 IF X3$(66,1)>"2" THEN PRECISION NUM(X3$(66,1),ERR=0246)
0295 DIM Z0$(80,X3$(235,1)); Z0$=MNM('GS')+Z0$+MNM('GE')
0300 REM "IOLISTS
0310 IOLIST A$,A[0],A[1],A[2]
0320 IOLIST B$
0330 IOLIST C$,C[0],C[1],C[2]
0500 REM "FILES
0505 DIM Z[NUM(X3$(60,3))]
0510 Z$="01O ZR1...  03O ZR0...  13O ZZPARM  "
0520 CALL "ZZFLES",X3$,Y1$,Y0$,Z$,Z{ALL},Z0,Z1; ON Z0 GOTO 0521,9900
0610 GOSUB 6000
0620 GOSUB 8100
0640 IF Q1$="" THEN CALL "ZZPROM",".Y",X3$,Z,"Proceed?","","",0; ON Z GOTO 0641,9900
1000 REM "BEGIN MAIN PROCESS
1020 REM "C=C+1; IF MOD(C,T0)=1 THEN GOSUB 8150
1030 U$=KEY(Z[1],END=5000)
1200 REM "READ DATA IN
1220 READ (Z[1],KEY=U$)IOL=0310
1230 GOSUB 1400
1250 FIND (Z[3],KEY=A$(1,10),DOM=1030)IOL=0330
1260 IF C[1]=0 THEN GOTO 1320; REM "IF # DAYS TO KEEP IS ZERO THEN DO NOT REMOVE
1290 GOSUB 4200
1310 IF A2$(1,6)>A$(53,6) THEN GOSUB 6500
1320 GOTO 1000
1400 REM "OPEN FILES AS NEEDED
1401 Z$=""
1405 IF F0$=A$(96,8) THEN GOTO 1490
1410 IF F0$<>"" AND F0$<>A$(96,8) THEN Z$="02C "+F0$ ELSE Z$=""
1420 F0$=A$(96,8),Z$=Z$+"02O "+F0$
1430 GOSUB 9750
1490 RETURN 
4200 REM "DATE CHECK
4210 JL$=X3$(21,6)
4250 CALL "ZZDATE",X3$,"",JL$,A2$,-C[1],W0,0,0,0,0,0,"","",""
4299 RETURN 
5000 REM "EOJ
5010 C=T; GOSUB 8150
5020 IF Q1$="" THEN CALL "ZZPROM",".4",X3$,Z,"Process complete!!","","",0
5040 GOTO 9900
6000 REM "BACKGROUND
6005 PRINT (0,ERR=6016)'SB',
6020 PRINT @(6,4),"This utility program will remove records from the Repository files",@(6,5),"based on the number of days to retain in the Repository Code Setup",@(6,6),"compared against todays date.  Are you sure you want to proceed?"
6165 PRINT (0,ERR=6166)'SF',
6190 RETURN 
6500 REM "DELETE RECORD
6525 REMOVE (Z[1],KEY=U$)
6540 INDEX$=A$(104,5)
6550 CALL "ZZREMR",Z[2],INDEX$
6599 RETURN 
8100 REM "GOSUB here, once, at the beginning, after the background is set and before the Proceed? question. Set T to total # to do (by calling ZZINFO) and T0 to the reporting interval (report every T0 number of records) this is typically 2% of total; This also prints a message (@8115),so adjust accordingly
8110 CALL "ZZINFO",Z[1],T9,X3$,T,T0,K,B,D,S0,S1,F$
8115 PRINT @(15,10),"There are "+STR(T)+" records to process" ! ssp#236018
8129 REM "Set T0, we make sure T0 is > 1, because later on we MOD and look for avalue of 1. IF T0 is 1, then nothing would get reported. We look for a result of 1 because this causes the first record to automatically start the reporting instead of waiting until the T0'th record to get the first report
8130 T0=INT(T*.02); IF T0<=1 THEN T0=2
8145 RETURN 
8150 REM "Call this each time to update the bar graph. Display horiz. bar graph given total #, T and current #, C, and time counter T1 (T1 is init'ed here and used here, just don't use it somewhere else)
8155 CALL "ZZBARG",X3$,"HG",19,10,50,T1,T,C
8195 RETURN 
8950 DEF FNS$(Z9$)=Z9$(1,POS("     "=Z9$+"     ")-1)
9000 REM "ERROR PROCESSING
9005 IF ERR=69 THEN GOTO 9500
9010 Y5=ERR,Y6=TCB(5)
9015 SETERR 9016; Y8$=LST(PGM(Y6))
9016 SETERR 9000
9040 CALL "ZZERRM",Y8$,X0$,Y7$,X3$,Y5,Y6,Y7,Y8,0
9045 REM 
9050 ON Y7 GOTO 9060,9100,9800,9070,9090
9055 REM 
9060 RETRY 
9070 SETERR 9080
9075 EXECUTE Y7$
9080 SETERR 9000; RETRY 
9090 SETERR 0000; RETRY 
9100 REM " TRANSFER CONTROL
9180 GOTO 0990
9190 GOTO 9800
9300 SETESC 9350
9310 SETERR 9350
9315 IF X3$(47,1)="N" THEN RETURN ELSE SETESC 0000; RETURN 
9350 SETERR 9000; RETURN 
9500 REM "CTRL LOGIC
9510 SETERR 9000; GOSUB 6400
9520 ON C9 GOTO 1140,2040
9750 REM "FILES
9760 CALL "ZZFLES",X3$,Y1$,Y0$,Z$,Z{ALL},Z0,Z1
9790 RETURN 
9900 REM "END PROGRAM
9910 CALL "ZZFLES",X3$,Y1$,Y0$,"END",Z{ALL},0,0
9930 SETERR 9940; Q1$=A1$; EXIT 
9940 SETESC 9350
9950 RUN "ZMENU"
9999 END 
